// App-level constants. Prompts are now in lib/orchestrator/prompts.ts.
// Re-exports for backward compatibility with frontend imports.

export { APP_NAME, DEFAULT_GREETING } from "@/lib/orchestrator/prompts";

export const MAX_MESSAGE_LENGTH = 600;
export const MAX_FEEDBACK_LENGTH = 300;
